version: '3.8'

services:
  code-solicitar:
    build: ./.docker
    container_name: code-solicitar
    restart: always
    tty: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - '3333:3333'
    networks:
      - db

networks:
  db:
    driver: bridge
    name: db
